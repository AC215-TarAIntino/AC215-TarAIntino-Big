[project]
name = "tarantaino-models"
version = "0.1.0"
description = "Lightweight heuristic recommender that consumes the TarAIntino processed dataset."
readme = "../../README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tarantaino-model = "models.pipeline:main"

[tool.uv]
package = true
