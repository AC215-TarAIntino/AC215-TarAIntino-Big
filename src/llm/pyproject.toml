[project]
name = "tarAIntino-llm"
version = "0.1.0"
description = "LLM chat interface using Google GenAI SDK"
requires-python = ">=3.11"

dependencies = [
    "google-genai>=0.3.0",
]

[tool.uv]
virtualenvs.in-project = true
