[project]
name = "tarAIntino-llm"
version = "0.1.0"
description = "LLM chat interface using Vertex AI Gemini"
requires-python = ">=3.11"

dependencies = [
    "google-cloud-aiplatform>=1.67.0",  # Vertex AI SDK
]
