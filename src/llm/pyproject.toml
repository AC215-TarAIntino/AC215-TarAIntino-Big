[project]
name = "tarAIntino-llm"
version = "0.1.0"
description = "Vertex AI LLM chat module for the TarAIntino project."
readme = "../../README.md"
requires-python = ">=3.11"

dependencies = [
    "google-cloud-aiplatform>=1.72.0",
]

[tool.uv]
virtualenvs.in-project = true
